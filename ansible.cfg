[defaults]
host_key_checking = False
inventory =  hosts # default host

enable_plugins = aws_ec2

remote_user = ec2-user
private_key_file = /home/montassar/devops-with-nana/ansible/ansible-ssh-key.pem 